# StudyPDF needs NO required environment variables — everything runs client-side.
# Optional client-side display overrides (baked into the build by Vite):
# VITE_MAX_UPLOAD_MB=100
# VITE_FILE_TTL_HOURS=24
